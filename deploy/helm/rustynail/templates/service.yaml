apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustynail.fullname" . }}
  labels:
    {{- include "rustynail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rustynail.selectorLabels" . | nindent 4 }}
