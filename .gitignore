# Rust
/target
*.rs.bk
*.pdb

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Diary (not committed to git - personal notes)
agenkit-learnings/
diary/

# Build artifacts
dist/
bin/

# quarry run records, if a run is ever pointed at the repo root rather than a
# configured `run_record_dir`. These are real artifacts of real runs and can carry
# problem statements and answers, so they must never be committed by accident.
quarry-run-*.json


# Added by cargo
#
# already existing elements were commented out

#/target
